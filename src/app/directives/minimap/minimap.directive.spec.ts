import { MinimapDirective } from './minimap.directive';

describe('MinimapDirective', () => {
  it('should create an instance', () => {
    const directive = new MinimapDirective();
    expect(directive).toBeTruthy();
  });
});
