import { CropDirective } from './crop.directive';

describe('CropDirective', () => {
  it('should create an instance', () => {
    const directive = new CropDirective();
    expect(directive).toBeTruthy();
  });
});
