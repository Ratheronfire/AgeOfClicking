import { SnapDirective } from './snap.directive';

describe('SnapDirective', () => {
  it('should create an instance', () => {
    const directive = new SnapDirective();
    expect(directive).toBeTruthy();
  });
});
