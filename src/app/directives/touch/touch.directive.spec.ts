import { TouchDirective } from './touch.directive';

describe('TouchDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchDirective();
    expect(directive).toBeTruthy();
  });
});
