import { SpritesheetElementDirective } from './spritesheet-element.directive';

describe('SpritesheetElementDirective', () => {
  it('should create an instance', () => {
    const directive = new SpritesheetElementDirective();
    expect(directive).toBeTruthy();
  });
});
