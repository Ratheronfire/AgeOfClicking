import { LongNumberPipe } from './long-number-pipe';

describe('LongNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LongNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
